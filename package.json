{
  "name": "roofscope",
  "version": "1.0.0",
  "description": "AI-Powered Roof Area Calculator using Mapbox and satellite imagery",
  "main": "server/index.js",
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "start": "cd server && npm start",
    "build": "npm run install:all && npm run build:client",
    "install:all": "npm run install:server && npm run install:client",
    "install:server": "cd server && npm install",
    "install:client": "cd client && npm install",
    "build:client": "cd client && npm run build && mkdir -p ../server/public && cp -r build/* ../server/public/",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm start",
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "test": "cd client && npm test"
  },
  "keywords": [
    "roof-area",
    "mapbox",
    "geospatial",
    "building-footprints",
    "react",
    "express"
  ],
  "author": "Anmol",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
